package core;

public class seedHandler {
    //more random
    //parse into the seed that's a string
    //make it into a long int
    //use when using Random random = new Random
    private long seed;
//    String [] seed;
}
